program
{
    int abc = 51, b = 6, c; /*abcd = abcd*/
    string x = "abce", y, z = "abcd";
    c = (abc + b) * 2;
    if (c >= 100 or x == z){
        read (y);
        write (y);
        write (x + y + z, c);
    }
    else{
        c = abc = 21;
    }
    while (c > 100 and b < 7){
        c = c - 5;
        write (c);
        x = x + "step";
        if (c ! 80){
            bool flag = true;
        }
    }

    for (int i = 0; i < c; i = i + 1){
        write(i);
    }


    hear : write(c);
    goto hear;
    
    write (x);
}/*abksda*/
